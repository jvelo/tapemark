# Implementation Plans

Generated by the improve skill on 2026-06-12, against commit `b61671a`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

These five were selected as the top findings by leverage. The advisor ran
**non-interactively** (no user available to choose), so this is the default
selection — adjust it freely. Additional vetted-but-unplanned findings are listed
under "Backlog" below; say the word to turn any into a plan.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Validate lookup `label` column (SQL injection fix) | P1 | S | — | DONE (in worktree, pending user merge) |
| 002 | Add a working typecheck gate (root + CI) | P1 | M | — | DONE (in worktree, pending user merge) |
| 003 | Enforce `hidden` tables server-side | P2 | S | — | DONE (in worktree, pending user merge) |
| 004 | HTTP-level tests for the CLI `serve` server | P2 | M | — | DONE (in worktree, pending user merge) |
| 005 | Harden form-body parsing + de-dupe CLI copy | P3 | S-M | 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **005 should land after 004.** Plan 004 adds black-box HTTP tests of the CLI
  `serve` server; plan 005 refactors that server's form-body parsing. Landing
  004 first gives 005 a regression net. (Soft dependency — 005 is doable alone,
  but riskier.)
- 001, 002, 003 are independent of each other and of 004/005; run them in any
  order. Suggested order is leverage-first (security and the typecheck gate
  first).

## Recommended sequence

1. **001** — security, smallest blast radius, highest leverage.
2. **002** — unblocks catching every future type regression; do early so later
   plans benefit from the gate.
3. **003** — closes the `hidden` access-control gap.
4. **004** — characterization tests for the CLI server.
5. **005** — harden + de-duplicate, on top of 004's net.

## Backlog — vetted findings not yet planned

Turn any of these into a plan on request.

- **No CSRF protection on state-changing POSTs** (security, MED conf). The panel
  embeds under a host app's cookie auth; create/update/delete/action POSTs have
  no token or origin check. Host-dependent and partly the integrator's
  responsibility, so it sits below the planned five — but worth an origin-check
  option or `SameSite` guidance. Evidence: no `csrf`/`origin`/`referer` handling
  anywhere in the request path.
- **D1 adapter has zero tests** (tests). `packages/db-adapters/d1/src/index.ts`
  is never exercised; failures only surface on Workers. Testable with a mock D1
  statement. Lower leverage than the CLI server gap (004).
- **`scripts` option undocumented; README options drift** (docs). `types.ts:296`
  exposes `scripts?: string[]`, absent from the README options block
  (`README.md:94-109`). Also `PHILOSOPHY.md:33,50` still says "Preact JSX" after
  the hono/jsx switch. Small, batchable.
- **`better-sqlite3` peer range `>=9.0.0` vs tested `^11`** (deps). The adapter's
  peer range is looser than what's actually tested; tighten or verify v9/v10.

## Findings considered and rejected

So nobody re-audits these:

- **`"col" IS ?` flagged as "invalid SQLite syntax"** in `row-view.tsx:166,208`
  — REJECTED. `IS ?` is valid SQLite and is *deliberately* used to NULL-match in
  the WITHOUT-ROWID update/delete fallback (`= ?` would never match NULLs). The
  code is correct.
- **Hono advisories from `pnpm audit`** — REJECTED as low-signal. `hono` is a
  host-controlled peer dependency, and the flagged advisories are in features
  tapemark doesn't use (`toSSG`, JWT, basic-auth, cookie middleware). The host
  app pins its own hono version.
- **`types.ts` "junk drawer" (354 lines)** — REJECTED. Coherent, grouped by
  concern, no dead/duplicate types.
- **Prettier / pre-commit hooks / per-package READMEs** — valid preferences but
  low leverage; not worth a plan unless the maintainer wants them. (Per-package
  READMEs have little value here since the registry is private/restricted.)

## Direction options (for the maintainer — not problems)

Grounded forward-looking suggestions; each would be a design/spike plan, not a
build-everything plan.

- **Sync** — PHILOSOPHY.md makes "Syncable" a core principle and the groundwork
  is laid (`schema.ts:246` computes a schema hash "for sync compatibility";
  the migrator persists `app_schema_hash`) but no pull/push exists. Strongest
  stated-but-undelivered signal.
- **List-view filtering/search** — the `_lookup` endpoint already does column
  search for FK pickers, but the main list view has only pagination. The
  philosophy promises Datasette-style "browse, filter, and visualize."
- **Turso/libSQL adapter** — PHILOSOPHY names "D1, Turso, libSQL"; only D1 and
  better-sqlite3 exist, and the adapter interface is ~48 lines.
